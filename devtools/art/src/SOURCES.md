# Source bundle

The OBJ and MTL files here are the generator's output as delivered to Rusty
(`Trailblazer_Automobility_Asset`, 2026-09), committed unchanged so the pipeline in
`build.py` is reproducible from the repository alone. The bundle's own atlas is not used:
the pipeline regenerates one from the MTL colours. Original work, all rights ours.
